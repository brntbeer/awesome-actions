name: Cats

on: [pull_request]

jobs:
  cats:
    runs-on: brntbeer-local
    if: ${{ github.event.pull_request.author == 'brntbeer'}}
    steps:
      - name: Action Cats
        uses: ruairidhwm/action-cats@master
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
